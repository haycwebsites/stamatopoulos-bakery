import { useTemplateInit } from '../lib/useTemplateInit';
// HAYC Pipeline: This file is overwritten during
// project creation with the client's navbar HTML
// converted to JSX.
// Source: <header> block from the HTML template.

export default function Navbar() {
  useTemplateInit();
  return (
    <header>
      {/* Navbar content injected by pipeline */}
    </header>
  );
}